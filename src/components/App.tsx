import { useState } from 'react'

function App() {
  const [count, setCount] = useState(0)

  return (
    <div className="App">
    just checking if this renders (app.tsx) 
    </div>
  )
}

export default App
